bind e "+forward; xhair_color"
bind s "+moveleft; xhair_color"
bind d "+back; xhair_color"
bind f "+moveright; xhair_color"

bind a "+c.speed"
bind z "+c.duck"

bind r +radar
bind g drop
bind w lastinv
// bind ctrl +voicerecord
bind space +voicerecord
bind shift player_ping


unbind mwheelup
bind mwheeldown "+jump"
bind ` toggleconsole
unbind TAB
unbind h
bind 4 ignorerad
bind 5 ignoremsg
bind mouse1 "+attack; r_cleardecals"

bind q "slot1"
bind c "slot2"
bind v "slot3"
bind 1 "fws.c4"
bind 2 "fws.molotov"
bind mouse3 "fws.he"
bind mouse4 "fws.flash"
bind mouse5 "fws.smoke"

bind ctrl +jumpthrow
