bind e +forward
bind s +moveleft
bind d +back
bind f +moveright

bind a "+c.speed"
bind z "+c.duck"

bind r +radar
bind g drop

bind mwheelup invprev
bind mwheeldown "+jump"
bind ` toggleconsole
unbind TAB
unbind h
bind 4 ignorerad
bind 5 ignoremsg
bind mouse1 "+attack; r_cleardecals"

bind q "fws.prim.sec"
bind v "fws.prim.knife"
bind 1 "fws.c4"
bind 2 "fws.molotov"
bind mouse3 "fws.he"
bind mouse4 "fws.flash"
bind mouse5 "fws.smoke"
